import { Router } from "express";

export const postRouter = Router();

postRouter.get("/", () => {});

postRouter.post("/", () => {});

postRouter.put("/", () => {});

postRouter.patch("/", () => {});

postRouter.delete("/", () => {});
